import { SlidingAnimationDirective } from './sliding-animation.directive';

describe('SlidingAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new SlidingAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
